DATA_PRESET:
  N_INTENTS: 4
  N_RESAMPLED_OBJ_POINTS: 2048

DATASET:
  TRAIN:
    DATA_SPLIT: train
    OBJ_CATES: all
    INTENT_MODE: ["use", "hold", "liftup", "handover"]


  VAL:
    DATA_SPLIT: val
    OBJ_CATES: all
    INTENT_MODE: ["use", "hold", "liftup", "handover"]

  TEST:
    DATA_SPLIT: test
    OBJ_CATES: all
    INTENT_MODE: ["use", "hold", "liftup", "handover"]
